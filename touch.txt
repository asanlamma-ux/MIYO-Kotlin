Based on my comprehensive analysis, I now have a complete picture of the 
situation. Let me create an extremely detailed prompt for you. ---
## 📋 COMPREHENSIVE PROJECT ANALYSIS & PROMPT
### Executive Summary
After 30+ failed builds, you've achieved build stability only to discover the 
**MIYU-Kotlin app is essentially a shell** — the data layer, native C++ EPUB 
engine, and infrastructure are complete (~40%), but the UI layer is severely 
lacking with only the Home screen implemented. The original React Native app 
(`~/EbookReaderOld`) contains **2,800+ lines of reader code, 20+ modals, 16 
themes, and 50+ features** that are completely missing from the Kotlin port. ---
## 🔴 THE CORE PROBLEM
### What You Built vs What You Expected
| Aspect | Current State (MIYU-Kotlin) | Expected State (React Native Reference) | 
|--------|------------------------------|-------------------------------------------|
| **Reader Screen** | ❌ **Directory exists, zero files** | ✅ 2,800+ lines of 
| WebView-based reader with text selection, highlights, bookmarks, TTS, 
| translation | **Library Screen** | ❌ **Empty placeholder** | ✅ Grid/list view, 
| import, OPDS, WTR-Lab, search, filters, storage management | **Settings Screen** 
| | ❌ **Empty placeholder** | ✅ 30+ settings categories, typography, themes, AI, 
| sync, navigation | **Dictionary Screen** | ❌ **Directory exists, zero files** | 
| ✅ Offline dictionary lookup, inline definitions | **Terms Screen** | ❌ **Empty 
| placeholder** | ✅ Term group management, MTL corrections, community downloads | 
| **Modals/Overlays** | ❌ **None** | ✅ 25+ modals (ThemePicker, BookAction, 
| Translation, AI Panel, etc.) | **Themes** | ⚠️ **4 defined but unused** | ✅ 16 
| themes with special effects (blossom, coffee, matcha) | **File Import** | ❌ 
| **No UI** | ✅ Document picker, SAF, watched folders, duplicate detection | 
| **Animations** | ❌ **Basic only** | ✅ Reanimated gestures, page transitions, 
| shared elements | **Icons** | ⚠️ **Material defaults** | ✅ Custom Lucide icons, 
| proper tab bar styling |
---
## 📊 DETAILED FEATURE COMPARISON
### Original React Native App (`~/EbookReaderOld`)
**Screens (5 tabs + reader):** 1. **Home** - Reading stats, continue reading 
carousel, daily goal, streak 2. **Library** - Grid/list books, import, OPDS 
catalog, WTR-Lab, filters 3. **Terms** - Term groups for MTL, community downloads, 
import/export 4. **History** - Grouped reading history with batch actions 5. 
**Settings** - Typography, themes, AI, sync, storage, permissions 6. **Reader** - 
Full EPUB reader with WebView (2,800+ lines) **Modals/Components (25+):** - 
ThemePickerModal, BookActionModal, BookDetailsModal - ImportSuccessModal, 
OpdsCatalogModal, WtrLabBrowserModal - SelectionToolbar, SearchInBookModal, 
AnnotationsDrawer - ReaderLayoutPanel, ReadingStatsModal, TranslationSheet - 
DictionaryLookupModal, InlineLookupModal, ReaderAiPanel - ReaderAiConfigModal, 
TermGroupDetail, AddTermModal - DictionaryLibraryModal, PermissionModal, 
AppDialog, LoggerModal **Features:** - EPUB parsing with JSZip - Chapter caching 
with WebView injection - Text selection with custom toolbar - Multi-color 
highlights with export - Bookmarks with notes - TTS with word highlighting - 
Translation overlay - Sleep timer, auto-scroll - Term replacement (MTL support) - 
16 themes with effects - OPDS catalog browser - WTR-Lab integration - Google Drive 
sync - WebDAV sync - Offline dictionaries - AI integration (OpenAI, Claude, 
Gemini) - Reading statistics - Cloud backup/restore **Design System:** - 
NativeWind (Tailwind for RN) - 16 built-in themes - Custom fonts (Crimson Pro, IBM 
Plex Sans, etc.) - Dynamic theming with CSS variables - Lucide icons - Reanimated 
animations
### Current Kotlin App (`/root/MIYU-Kotlin`)
**Implemented:** - ✅ MainActivity with Compose setup - ✅ MIYUApp with 
BottomNavigationBar (5 tabs) - ✅ HomeScreen with BookCard and progress indicator 
- ✅ MIYUTheme with light/dark mode - ✅ 4 reader themes defined (White, Sepia, 
Dark, Forest) - **unused** - ✅ Room database (8 entities, full schema) - ✅ 
Repository layer (BookRepository, TermRepository) - ✅ DataStore preferences 
(UserPreferences.kt) - ✅ Native C++ EPUB engine (570+ lines, JNI bridge) - ✅ 
Hilt DI setup - ✅ Navigation scaffold **Missing (All UI Screens):** - ❌ 
**LibraryScreen** - Empty placeholder in `ui/library/LibraryScreen.kt` - ❌ 
**TermsScreen** - Empty placeholder in same file - ❌ **HistoryScreen** - Empty 
placeholder in same file - ❌ **SettingsScreen** - Empty placeholder in same file 
- ❌ **ReaderScreen** - Directory exists at `ui/reader/`, **zero files** - ❌ 
**DictionaryScreen** - Directory exists at `ui/dictionary/`, **zero files** - ❌ 
**OPDSScreen** - Directory exists at `ui/opds/`, **zero files** - ❌ **All 
modals** - `ui/components/` directory exists, **zero files** **Native C++ 
Implementation (Complete):** - ✅ EPUB parser (metadata, chapters, CSS) - ✅ Cover 
extraction (base64) - ✅ Chapter rendering with term replacement - ✅ In-memory 
caching - ❌ MOBI/PDF/Comic engines (directories exist but empty) ---
## 🛠️ BUILD JOURNEY ANALYSIS
### 30+ Failed Builds → Stability
The git history shows a **classic greenfield CI/CD odyssey**:
| Phase | Commits | Issues Fixed | -------|---------|--------------| **Phase 1** | 
| `26766f0` - `da0cd34` | Gradle/JVM opts conflicts | **Phase 2** | `fe80447` - 
| `6a443ca` | C++ `std::unique_ptr` with incomplete type, header paths | **Phase 
| 3** | `de48a08` - `481894c` | Dependency resolution (Supabase/Ktor) | **Phase 
| 4** | `c40c7f3` | Keystore signing (base64 decode failures) | **Phase 5** | 
| `5885738` - `584585c` | R8/ProGuard (562 lines of rules needed) |
**Current Stable Configuration:** - Multi-stage pipeline (lint → test → build → 
release) - Defensive keystore handling with 3 fallback decode methods - 
Comprehensive ProGuard rules (562 lines) - NDK 26.1.10909125 pinned - Gradle with 
Kotlin DSL (AGP 8.2.2, Kotlin 2.0.0) **Remaining Fragile Areas:** 1. **Keystore 
files in repo** (`release.keystore`, `test.keystore`) - security risk 2. 
**Hardcoded NDK version** - may break with updates 3. **R8 full mode** - 
aggressive obfuscation requires extensive rules 4. **Keystore decode** - currently 
using chained `||` which can corrupt file ---
## 🎨 DESIGN & IMPLEMENTATION GUIDANCE
### Vector Graphics in Kotlin/Android
**Jetpack Compose Approach (Recommended):** ```kotlin
// Material Icons (built-in)
import androidx.compose.material.icons.Icons import 
androidx.compose.material.icons.filled.Home import 
androidx.compose.material.icons.outlined.Settings Icon(
    imageVector = Icons.Default.Home, contentDescription = "Home" )
// Custom SVG as ImageVector
val CustomLogo: ImageVector = ImageVector.Builder( defaultWidth = 24.dp, 
    defaultHeight = 24.dp, viewportWidth = 24f, viewportHeight = 24f
).apply {
    // path operations
}.build()
``` **For Lucide Icons (matching React Native):** - Use 
`androidx.compose.material:material-icons-extended` - Or import Lucide icons as 
SVG and convert to `ImageVector`
### Best Practices for Kotlin/Android 2024-2025
1. **Jetpack Compose** - Official recommendation, XML is maintenance mode 2. 
**Material Design 3** - Use from the start, all new features are Compose-first 3. 
**ViewModel + StateFlow** - Replace React Context/Redux 4. **Navigation Compose** 
- Type-safe with Navigation 2.8.0+ 5. **Hilt for DI** - Cleaner than manual 
injection 6. **Coil for images** - Replacement for React Native Image ---
## 📝 COMPREHENSIVE PROMPT FOR IMPLEMENTATION
### Task: Implement Missing UI Layer for MIYU Ebook Reader
**Context:** You have a partially complete Android/Kotlin ebook reader 
(MIYU-Kotlin). After 30+ failed builds, the build system is now stable and the app 
compiles successfully. However, the UI layer is severely incomplete — only the 
Home screen is implemented with actual content. The data layer, native C++ EPUB 
engine, and infrastructure are production-ready. **Reference Implementation:** 
Study the original React Native app at `~/EbookReaderOld` which contains: - 2,800+ 
line Reader screen with WebView-based EPUB rendering - 25+ modals and overlay 
components - 16 themes with special effects (blossom, coffee, matcha, comfort) - 
Complete library management with OPDS/WTR-Lab integration - Full settings panel 
with 30+ categories - Term management system for MTL novels - Dictionary lookup 
system - AI integration panel **Current State Analysis (MIYU-Kotlin):** - **Build 
System**: Stable (Kotlin DSL, AGP 8.2.2, Compose BOM 2024.05.00) - 
**Architecture**: MVVM + Repository + Hilt DI - **Data Layer**: Complete (Room 
with 8 entities, DataStore preferences) - **Native Layer**: C++ EPUB parser with 
JNI (complete) - **UI Layer**: 40% complete
  - ✅ HomeScreen with BookCard - ✅ Navigation scaffold (5 tabs) - ✅ Theme 
  system (light/dark, 4 reader themes defined) - ❌ LibraryScreen (placeholder) - 
  ❌ ReaderScreen (directory exists, empty) - ❌ SettingsScreen (placeholder) - ❌ 
  TermsScreen (placeholder) - ❌ HistoryScreen (placeholder) - ❌ DictionaryScreen 
  (directory exists, empty) - ❌ All modals/components (directory exists, empty)
**Requirements:** 1. **Library Screen** (`ui/library/LibraryScreen.kt`) - Grid and 
   list view toggle for books - Search functionality - Filter by status (unread, 
   reading, finished) - Sort options (recent, title, author, progress) - EPUB 
   import button with document picker - OPDS catalog browser (prepare for 
   `ui/opds/`) - WTR-Lab integration (prepare for future) - Book long-press 
   actions (delete, mark status) - Empty state matching React Native design - Use 
   Coil for book cover images
2. **Reader Screen** (`ui/reader/ReaderScreen.kt`) - WebView-based EPUB chapter 
   display - Chapter navigation (previous/next) - Text selection with floating 
   toolbar - Bookmark current chapter - Highlight text with color picker - Theme 
   switcher (use existing White/Sepia/Dark/Forest) - Progress indicator - Chapter 
   drawer with list - Text search within book - Integrate with native C++ EPUB 
   engine via JNI
3. **Settings Screen** (`ui/settings/SettingsScreen.kt`) - Account section (sign 
   in/out with Supabase) - Typography settings (font, size, line height, 
   alignment) - Reading settings (animations, tap zones, margins) - Theme 
   selection (16 themes matching React Native) - Cloud sync (Google Drive, WebDAV 
   placeholders) - AI configuration (OpenAI, Claude, Gemini placeholders) - 
   Storage management - About section
4. **Terms Screen** (`ui/terms/TermsScreen.kt`) - Term group cards with count - 
   Create/delete term groups - Term group detail view with edit - Community term 
   group browser - Import/export JSON
5. **History Screen** (`ui/history/HistoryScreen.kt`) - Grouped by time (Today, 
   Yesterday, This Week, etc.) - Book progress display - Selection mode for batch 
   removal
6. **Components/Modals** (`ui/components/`) - ThemePickerModal - BookActionModal - 
   ReaderLayoutPanel - AnnotationsDrawer - SearchInBookModal - 
   DictionaryLookupModal
**Design Requirements:** 1. **Match React Native Reference** - Study 
`~/EbookReaderOld` for:
   - Color schemes (use existing `Color.kt` as base) - Typography scale (use 
   Material3) - Iconography (Material Icons Extended) - Spacing and layout 
   patterns - Animation patterns (use Compose Animation APIs)
2. **Implement 16 Themes** - Port from React Native: - Sepia Classic, Night Mode, 
   Forest Green, Lavender Dream - Midnight OLED, Parchment Comfort, Monochrome, 
   Ocean Blue - Warm Sunset, Nordic Night, Peach Blossom, Dark Coffee - Matcha 
   Paper, Ink Stone, Blueprint Day, Ember Night
3. **Vector Icons** - Replace basic Material icons: - Use Material Icons Extended 
   where possible - Import custom icons as ImageVector if needed - Match Lucide 
   icon set from React Native
**Technical Constraints:** 1. **Use existing architecture** - Follow established 
patterns:
   - MVVM with ViewModels in `viewmodel/` - Repository pattern (BookRepository, 
   TermRepository exist) - Hilt for dependency injection - Compose with Material3
2. **Integrate with native layer**: - EPUB parsing via `EpubEngineBridge` (JNI) - 
   Book covers already extracted to database - Reading position via 
   `ReadingPositionEntity`
3. **Maintain build stability**: - Do not modify `.github/workflows/build.yml` - 
   Do not modify `gradle/` configuration - Keep ProGuard rules intact
4. **Follow Kotlin conventions**: - Use StateFlow for UI state - Use 
   collectAsStateWithLifecycle() - Use Modifier chains for styling - Follow 
   official Compose samples (Now in Android)
**Success Criteria:** - All 5 tab screens have functional implementations - Reader 
screen can open EPUBs and display chapters - Library screen shows book grid with 
covers - Settings screen has all 30+ categories from React Native - 16 themes are 
selectable and apply correctly - Bottom navigation matches React Native styling - 
App compiles and runs without build errors - No placeholder screens remain 
**Output:** Provide the complete implementation for all missing screens, 
components, and themes. Ensure the UI matches the React Native reference app in 
terms of features, layout, and user experience, adapted to Android/Material3 
design patterns. ---
This prompt is ready for execution by an AI agent or development team. It provides complete context, clear requirements, and specific technical guidance based on the thorough analysis of both codebases.
